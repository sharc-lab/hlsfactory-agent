# ArrayTest (GPU Task)

Implements an array processing task for the Blaze framework, designed to run on GPU backends.
The `ArrayTest` class inherits from `blaze::Task` and overrides the `compute()` method.
