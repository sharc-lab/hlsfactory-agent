# convenient way to run docker container - docker-compose up
# version: '3.8'

services:
  hls-organizer:
    build: . # . finds the dockerfile from the current directory
    environment:
      # - SOURCE_REPO=${SOURCE_REPO}
      - OUTPUT_DIR=${OUTPUT_DIR:-/output}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      
