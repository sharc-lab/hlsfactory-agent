# Pi Design

Implements a Monte Carlo Pi estimation task using the Blaze framework.
The `Pi` class inherits from `blaze::Task` and overrides the `compute()` method to count points inside and outside the unit circle.
