# Design `tapa_qcf`

This design implements the HLS kernel defined in `tapa_qcf.cpp`. It uses the common header files from the repository's `fpga/include` directory. A basic testbench `testbench.cpp` is provided.
