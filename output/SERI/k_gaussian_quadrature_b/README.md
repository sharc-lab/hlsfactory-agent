# Design `k_gaussian_quadrature_b`

This design implements the HLS kernel defined in `k_gaussian_quadrature_b.cpp`. It uses the common header files from the repository's `fpga/include` directory. A basic testbench `testbench.cpp` is provided.
