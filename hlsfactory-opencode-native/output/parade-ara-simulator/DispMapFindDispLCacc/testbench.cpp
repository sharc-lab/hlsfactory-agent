// Testbench for DispMapFindDispLCacc
// Auto-generated by HLSFactory

#include "DispMapFindDispLCacc.h"
#include <iostream>
#include <cmath>
#include <cstdlib>

// Stub declarations for testing without full simulator
#ifndef LCAcc_Command
#define LCAcc_Command(t, id, cmd, buf, size, a, b) {}
#endif
#ifndef LCAcc_Reserve
#define LCAcc_Reserve(t, id, c) {}
#endif
#ifndef LCAcc_Request
#define LCAcc_Request(t, mode, count) {}
#endif
#ifndef LCAcc_Free
#define LCAcc_Free(t, id) {}
#endif
#ifndef LWI_RegisterInterruptHandler
#define LWI_RegisterInterruptHandler(args) {}
#endif
#ifndef LWI_UnregisterInterruptHandler
#define LWI_UnregisterInterruptHandler(t, id) {}
#endif
#ifndef LWI_CheckInterrupt
#define LWI_CheckInterrupt(t) nullptr
#endif
#ifndef LWI_ClearInterrupt
#define LWI_ClearInterrupt(t) {}
#endif
#ifndef simics_assert
#define simics_assert(x) { if(!(x)) { std::cerr << "Assertion failed: " << #x << std::endl; } }
#endif
#ifndef Touch
#define Touch(t, buf, size) {}
#endif

// Test function
int test_DispMapFindDispLCacc() {
    std::cout << "Testing DispMapFindDispLCacc..." << std::endl;
    
    // Allocate test data
    const int size = 1024;
    const int chunk = 64;
    
    float* in_a = new float[size];
    float* in_b = new float[size];
    float* out = new float[size];
    
    // Initialize test data
    for(int i = 0; i < size; i++) {
        in_a[i] = static_cast<float>(i);
        in_b[i] = static_cast<float>(i * 2);
        out[i] = 0.0f;
    }
    
    // Call accelerator function (if available)
    // CreateBuffer_DispMapFindDispLCacc_sig(0, in_a, in_b, out, size, chunk);
    
    std::cout << "DispMapFindDispLCacc test completed (stubs used)" << std::endl;
    
    delete[] in_a;
    delete[] in_b;
    delete[] out;
    
    return 0;
}

int main() {
    std::cout << "==============================================" << std::endl;
    std::cout << "Testbench for DispMapFindDispLCacc" << std::endl;
    std::cout << "==============================================" << std::endl;
    std::cout << std::endl;
    
    int result = test_DispMapFindDispLCacc();
    
    std::cout << std::endl;
    std::cout << "==============================================" << std::endl;
    if (result == 0) {
        std::cout << "TEST PASSED" << std::endl;
    } else {
        std::cout << "TEST FAILED" << std::endl;
    }
    std::cout << "==============================================" << std::endl;
    
    return result;
}
